"use strict";

// Source candidate only. The DevTools upload script rewrites this module in
// isolated clean staging so the shipped About page matches the final upload ID.
module.exports = Object.freeze({
  version: "2026.07.19.001",
  buildDate: "2026-07-19",
});
