<!-- CODE_ALIGNMENT_2026_04_30_START -->
> Code alignment note (2026-04-30): this document was reviewed during the repository-wide docs/code sync. Treat `README.md`, `AGENTS.md`, `docs/CURRENT_CODE_MAP.md`, `docs/CLI_REFERENCE.md`, `docs/CONFIGURATION.md`, `docs/CODE_AUDIT.md`, and `docs/DOCUMENTATION_INDEX.md` as current code-aligned SSOT. Older dated handoffs/logs remain historical evidence and must be checked against current code before execution.
<!-- CODE_ALIGNMENT_2026_04_30_END -->

# 🔴 WATCHDOG RED REPORT — 2026-04-28 04:14 +08

## 综合判定：RED（llama-swap 不可达 ×3，llama-server 进程已消失）

## 升级路径
```
02:32 preflight  → AMBER (llama-swap 超时, llama-server LISTENING)
03:38 watchdog   → AMBER (llama-swap 超时, llama-server LISTENING) — 2nd failure
04:14 watchdog   → RED   (llama-swap 超时, llama-server GONE)     — 3rd failure ⚠️
```

## 详细检查

| 检查项 | 状态 | 详情 |
|--------|------|------|
| run-state.json | ✅ GREEN | running, P0 Day 0 Setup, US-000/US-001 完成 |
| D盘剩余空间 | ✅ GREEN | 8.4TB / 15TB (43%) |
| llama-swap:11434 | ❌ RED | HTTP_CODE:000, Exit 7 — 3rd consecutive |
| **llama-server:11433** | ❌ **RED** | **PID 50800 消失！** 03:38 时尚在 LISTENING |
| checkpoint 新鲜度 | ⚠️ AMBER | ~64min (last 03:10, now 04:14) |
| 危险进程扫描 | ✅ GREEN | 无递归扫描 |
| 输出目录 | ✅ GREEN | 0 字节 (Day0 预期) |
| WeChat 进程 | ⚠️ AMBER | 未运行 |

## 根因分析

### llama-server 崩溃
- **03:38 状态**: PID 50800 (llama-server.exe) 监听 127.0.0.1:11433，加载 Qwen3.6-27B-Q4_K_M.gguf
- **04:14 状态**: PID 50800 不存在，端口 11433 无监听
- **llama-swap (PID 30772)**: 仍运行，对 Windows `Get-Process` 响应为 `Responding: True`，但端口 11434 的 HTTP 请求超时（因后端 server 已死）
- **netstat 确认**: 14 个 TIME_WAIT 连接到 11434 → 大量尝试连接但 HTTP 层未响应
- **ESTABLISHED 连接**: 127.0.0.1:11434 ↔ 127.0.0.1:3384 (PID 36544 → PID 30772)，可能是健康检查连接

### 时间线推断
- llama-server 在 03:38 ~ 04:14 之间崩溃
- 崩溃原因不明（GPU OOM / 模型加载失败 / 内部状态死锁后退出）

## RED 触发依据
run-state.json 规则：`"model unreachable 3x consecutive"` → RED

| # | 时间 | 来源 | 验证方式 |
|---|------|------|----------|
| 1 | 02:32 | preflight | WSL curl 超时, Windows curl 超时 |
| 2 | 03:38 | watchdog | WSL curl 超时 ×2, netstat 确认 LISTENING 但无响应 |
| 3 | 04:14 | watchdog | WSL curl 超时, llama-server 进程消失 |

## 影响评估
- **下游管线**: 完全阻塞 — 所有需要本地推理的阶段（Qwen3.6 校准、downstream_matrix_50 评估）无法执行
- **Day0 不受影响**: US-000/US-001 已完成，Day0 剩余工作（US-002 Baseline Freeze）可推迟
- **cron 心跳**: 本 watchdog 和 nightwatcher 继续运行（只读），但 daily-executor (12:00) 将无法推进 LLM 依赖的 story

## 硬规则检查
- ✅ 无 D:\DDownload 写入/修改
- ✅ 无 git reset/clean/删除
- ✅ 无 Docker prune/删除
- ✅ 无禁止进程
- ✅ 本报告仅写入 reports/ 目录，未修改任何 live root

## 建议操作（人工）
1. **检查 Windows 事件查看器**: 查找 llama-server.exe 崩溃事件（Application Error 事件 ID 1000）
2. **检查 GPU 状态**: `nvidia-smi` 确认 VRAM 和 GPU 是否正常
3. **重启 llama-swap**: 重新启动 llama-swap 服务（会连带启动 llama-server）
4. **验证恢复**: 重启后 `curl http://127.0.0.1:11434/v1/models` 确认返回模型列表

## REVIEW LOOP（反思层）

### Mini Review
llama-swap 不可达问题在 ~1.5 小时内从 AMBER（端口在监听但无响应）升级到 RED（后端 server 进程崩溃消失）。前两次心跳正确识别了异常但受限于"只读监控、不自动修复"的规则。DeepTutor 反思层在上次报告（03:38）中已建议增加自动重启逻辑，本次崩溃验证了该建议的必要性。

### 需要调整的事项
- **提示词/rules**: 在 `run-state.json` 的 `red_criteria` 中增加 `"model server process gone"` 作为独立 RED 触发条件（当前仅依赖 "3x consecutive unreachable"）
- **恢复策略**: 考虑在非 Day0 时段增加一次自动重启尝试（`Restart-Service llama-swap` 或等价命令），而非等待人工介入
- **monitor 增强**: nightwatcher-cli.mjs 可增加 `llama-server.exe` 进程存活检查，区分"端口监听但无响应"和"后端崩溃"两种故障模式

## 决策
- **decision: RED**
- **need_human: true**
- **action**: 已停住，等待人工恢复 llama-swap/llama-server
- **下次 watchdog**: ~04:44 +08 (继续监控，但 RED 状态不变直到恢复)
