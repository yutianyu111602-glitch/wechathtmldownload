<!-- CODE_ALIGNMENT_2026_04_30_START -->
> Code alignment note (2026-04-30): this document was reviewed during the repository-wide docs/code sync. Treat `README.md`, `AGENTS.md`, `docs/CURRENT_CODE_MAP.md`, `docs/CLI_REFERENCE.md`, `docs/CONFIGURATION.md`, `docs/CODE_AUDIT.md`, and `docs/DOCUMENTATION_INDEX.md` as current code-aligned SSOT. Older dated handoffs/logs remain historical evidence and must be checked against current code before execution.
<!-- CODE_ALIGNMENT_2026_04_30_END -->

# 🔍 DeepTutor Prompt Review — 2026-04-28 09:09 +08

**Cycle:** #1 (first 6h review cycle)
**Run:** hermes-7day-2026-04-28
**Reviewer:** prompt-review-loop cron (691cb6c6fd32)

---

## SOLVE LOOP：本轮数据收集

### 1. Run State 快照

| 字段 | 值 |
|------|-----|
| run_id | hermes-7day-2026-04-28 |
| status | running (mtime 03:10, **5h59m 陈旧**) |
| current_phase | P0 — Day 0 Setup |
| stories_completed | US-000, US-001 |
| next_story | US-002 Baseline Freeze |
| llama-swap | AMBER → RECOVERED (07:38) |
| WeChat | RED (not running) |
| mem0 | AMBER (quota exceeded, resets May 1) |

### 2. Watchdog 报告时间线（14 份报告，7.5h 覆盖）

```
02:32  preflight         AMBER  (llama-swap timeout #1)
03:05  watchdog          GREEN  (initial baseline)
03:38  watchdog          AMBER  (llama-swap zombie #2, server LISTENING but no HTTP response)
04:14  watchdog 🔴       RED    (llama-server CRASHED PID 50800 gone, 3rd consecutive unreachable)
04:33  [auto-recovery]   llama-server PID 43864 spawned — GREEN but alias lost
05:18  watchdog          AMBER  (downstream batch 0% success detected)
05:53  watchdog          AMBER  (24,969 failed, 0 succeeded — confirmed systemic)
06:28  watchdog          AMBER  (30,380 failed, accelerating at 155/min)
07:03  watchdog          AMBER  (36,098 failed, ROOT CAUSE: model name mismatch 'Qwen3.6-27B')
07:38  watchdog          AMBER  (42,225 failed, FIX APPLIED server-side, batch needs restart)
08:18  watchdog          AMBER  (47,661 failed, fix NOT applied to batch, still burning)
08:56  watchdog          AMBER  (status file DISAPPEARED, 3 zombie node.exe remain)
```

### 3. Cron 执行统计

| 指标 | 值 |
|------|-----|
| 总 cron session 文件 | 308 |
| run-state 创建后的新 session | 38 |
| 预计 cron 触发次数（7.5h） | ~45 (nightwatcher 10m ×45 + watchdog 30m ×14 + 本 review ×1) |
| 实际 watchdog 报告产出 | 11 (100% yield from watchdog cron) |

---

## REVIEW LOOP：自我批判 + 提示词优化

### 4. 重复出现的 AMBER/RED 模式评估

| 模式 | 首次发现 | 持续时长 | 状态 |
|------|----------|----------|------|
| **llama-swap 不可达** | 02:32 preflight | 1h42m → RED at 04:14 | ✅ 已自愈 (04:33) |
| **downstream batch 0% 成功率** | 05:18 | **3h51m and counting** | 🔴 仍在恶化 |
| **run-state 心跳缺失** | 05:53 (3h stale) | **5h59m** | 🔴 持续 |
| **Qwen3.6-27B 别名丢失** | 04:33 | 2h30m → fixed at 07:03 | ✅ 已修复 |

**关键发现**: 07:03 诊断出根因并 35min 内在服务器端修复，但 batch 进程从未重启，导致修复收益为零。这是"已知修复未应用"的典型案例，暴露了自动化管线的执行盲点。

### 5. 执行效率：过去 6h 推进评估

| 指标 | 值 | 评价 |
|------|-----|------|
| stories 完成 | 0 (US-000/US-001 在 Day0 03:10 完成，之后零推进) | ⚠️ 停滞 |
| US-002 进度 | 未启动 | ⚠️ |
| downstream batch | 47,661/93,000 被错误标记失败 | 🔴 净损失 |
| llama-swap 恢复收益 | 0（batch 未重启） | 🔴 零转化 |
| checkpoint 写入 | 0 (自 03:10) | ⚠️ |

**根因**: 自主运行器在 Day0 Setup 后停止（非持续循环）。daily-executor 首次触发在 12:00（尚有 2h51m）。停滞是设计问题而非故障——跑者是单次 setup，后续推进依赖 cron 的 daily-executor。

### 6. 提示词自检：是否需要调整？

#### 6a. Watchdog 提示词 (hermes-7day-watchdog, ed27b1cfb547)

**当前质量**: ✅ 良好 — 产出结构化、根因定位准确、包含升级路径

**发现的盲点**:
1. ❌ **缺少 batch 成功率 RED 触发**：当前 RED 条件为 "model unreachable 3x"，但 batch 0% 成功率持续 4h 未触发 RED。应在 watchdog 的 Review Loop 中增加 "batch 0% success > 60min → AMBER, > 3h → RED trend warning"
2. ❌ **缺少"已知修复未应用"升级逻辑**：07:03 根因定位后，watchdog 知道修复路径但未升级严重性。应增加 "fix_known_not_applied > 2 cycles" 的升级条件
3. ⚠️ **WSL curl 到 Windows localhost 检查冗余**：多份 watchdog 使用 WSL curl → Windows PS Invoke-WebRequest 双重验证，05:53 后已知 WSL 侧有网络隔离限制，后续 watchdog 继续在 WSL 侧 FAILED 但 Win 侧 GREEN。建议统一使用 Win PS 验证

#### 6b. Daily-executor 提示词 (hermes-7day-daily-executor, 1509f1c94d40)

**待补充（首次运行前审查）**:
1. ❌ **缺少 step 0：僵尸进程清理**：当前提示词未包含检查并清理孤儿 batch 进程。应在所有 story 推进前增加：
   - 检查 downstream-batch-status.json
   - 若 succeeded=0 且运行 > 2h → kill 进程，写证据
   - 验证 llama-swap Qwen3.6-27B 模型存在
   - 确认后可选 `--resume` 重启 batch
2. ❌ **缺少 run-state 心跳更新**：daily-executor 应在每步操作后更新 `last_updated`，当前设计未包含
3. ⚠️ **story 跳过逻辑不完整**：当前自适应策略只考虑 WeChat/llama-swap DOWN 时跳过对应 story，未考虑"下游 batch 僵尸占用资源"的情况

#### 6c. 自主运行器 Day0 提示词

**已确认问题**: Day0 setup 提示词设计为单次执行，完成后停止。这是设计意图。但缺少将 `next_story` 和周期性 check 交接给 daily-executor 的明确 handoff 步骤。

### 7. 提示词补丁建议

#### Patch #1: Watchdog — 增加 batch 成功率 RED 触发条件

**目标文件**: watchdog cron prompt（通过 `hermes cron update` 或重新创建）

**新增检查项**（在 Review Loop 第 6 步之后增加）:

```
7. **BATCH 成功率检查**（新增）:
   - 读取 downstream-batch-status.json（若存在）
   - 若 status=running 且 succeeded=0：
     * 运行 < 60min → 记录 INFO
     * 运行 60-180min → AMBER "batch 0% success, monitor"
     * 运行 > 180min → RED trend "batch burning queue, fix known but not applied"
   - 若 status file missing 但 node.exe 进程存活 → AMBER "zombie batch, orphan processes"
   - 若 both missing → GREEN (batch cleaned up)
```

#### Patch #2: Daily-executor — 增加 Step 0 僵尸清理 + 心跳

**目标文件**: daily-executor cron prompt

**在 SOLVE LOOP 第 1 步前插入**:

```
**STEP 0: 僵尸进程清理 + 环境验证（新增）**

0a. 检查 downstream batch 状态:
    - 读取 D:\DDownload\_downstream_llm\downstream-batch-status.json
    - 若 status=running 且 succeeded=0 且运行 > 2h:
      → powershell "Get-Process node | Where-Object {$_.StartTime -lt (Get-Date).AddHours(-2)} | Stop-Process -Force"
      → 写 evidence 到 reports/zombie_cleanup_{timestamp}.md
      → 记录: 已终止 N 个孤儿 batch 进程
    
0b. 验证 llama-swap 模型可用性:
    → powershell "(Invoke-WebRequest http://127.0.0.1:11434/v1/models).Content | findstr Qwen3.6"
    → 若不可用: 降级执行分析/规划任务（见自适应策略）

0c. 更新 run-state.json:
    → last_updated: {now}
    → 若 llama-swap GREEN: 标记可用
    → 若下游 batch 已清理: 记录
```

#### Patch #3: anti_blocking rules — 增加 batch 相关 RED 条件

**目标**: `run-state.json` → `anti_blocking.red_criteria`

```json
"red_criteria": [
  "disk<50GB",
  "data corruption", 
  "same story fail 3x",
  "model unreachable 3x consecutive",
  "downstream batch 0% success > 3h with fix known but not applied"
]
```

### 8. 已知但未解决的系统性问题

| 问题 | 严重度 | 影响 | 建议 |
|------|--------|------|------|
| batch 进程不重新验证模型 | 中 | 服务器端修复后 batch 不获益 | 需要 batch 代码改进（非本 review 范围） |
| WSL2 → Windows localhost 网络隔离 | 低 | 导致双重检查冗余 | 统一用 Win PS 验证，减少误报 |
| mem0 quota exceeded | 低 | 无法保存学习到 mem0 | 5月1日自动恢复 |
| nightwatcher 危险扫描误报 `-r` | 低 | 匹配 `--resume` 等合法参数 | 修正为 `\b-r\b` (已在 05:53 建议) |

---

## 决策与建议

```
prompt-review: 2026-04-28T09:09:00+08:00
cycles_this_period: 1 (首次执行)
watchdog_status: AMBER (7次连续AMBER, 1次RED已恢复)
stories_completed: 0 (本周期)；累计 US-000, US-001
blocking_issues:
  - downstream batch 僵尸进程 (3x node.exe PIDs 32180/47092/15920, 状态文件已消失)
  - run-state.json 5h59m 未更新
  - US-002 Baseline Freeze 未启动（等待 daily-executor 12:00）
prompt_changes_needed: true
suggested_patches:
  - Watchdog: 增加 batch 成功率 RED 触发 (运行 > 3h + succeeded=0 → RED trend)
  - Daily-executor: 增加 Step 0 僵尸清理 + 环境验证 + run-state 心跳更新
  - run-state.json: red_criteria 增加 batch 相关条件
next_cycle_actions:
  - 每日执行器 (12:00) 应首先清理 3 个僵尸 node.exe
  - 验证 llama-swap Qwen3.6-27B 持久化
  - 推进 US-002 Baseline Freeze
  - 将本报告建议的 patch 整合到 daily-executor 第一轮执行中
  - 下次 prompt-review: ~15:09 +08
review_saved_to: reports/prompt-review-2026-04-28_0909.md
```

---

**NOTE**: 本报告只读分析。所有提示词修改建议等待 daily-executor (12:00) 或人工审查后整合。不执行任何 cron prompt 修改。
