# 🔍 DeepTutor Prompt Review — 2026-05-04 16:53 +08

```text
prompt-review: 2026-05-04T16:53:00+08:00
cycles_this_period: 9 watchdog samples in last 6h (1048-1645); cron_history_matches=1110
watchdog_status: RED (7次), AMBER (2次), GREEN (0次); latest3=AMBER/AMBER/RED
stories_completed: 0
blocking_issues:
  - STAGE7/93K BATCH001 COMPLETED BUT RELAUNCH CONFIRMED: BATCH001 shards 001-004 finished (~15:56), commander self-terminated. BUT shard005 auto-started at 16:43 (MAINTENANCE_WINDOW_MISSED). This proves the OpenClaw control plane can still launch new runners beyond BATCH001 scope. Recovery gate #36 NOT satisfied.
  - RUN-STATE STALE 4.8h: last_updated=12:02, does not reflect BATCH001 completion or shard005 auto-start. Daily executor runs only at 12:00, so afternoon state changes go unrecorded until tomorrow.
  - PATCH #36 RECOVERY CRITERIA INSUFFICIENT: #36 requires "no new batch/shard/run-id since last RED" but doesn't explicitly handle the MAINTENANCE_WINDOW_MISSED pattern (shard auto-start after all prior shards complete). The shard005 auto-start at 16:43 is a NEW run-id advancement after the RED→AMBER transition, invalidating the AMBER recovery samples.
  - WECHAT STILL RED (process_count=0): US-003 capture remains blocked regardless of Stage7 status.
  - MEM0 UNAVAILABLE: Provider: (none — built-in only). No learnings persisted across sessions.
prompt_changes_needed: true
suggested_patches: |
  CONSOLIDATED PATCH #37 — MAINTENANCE_WINDOW_MISSED RELAUNCH GUARD + RUN-STATE FRESHNESS.
  Supersedes/extends #36 recovery gate. Incorporates shard005 auto-start observation.

  ## Part A: Watchdog — MAINTENANCE_WINDOW_MISSED → escalate to RED

  When watchdog detects MAINTENANCE_WINDOW_MISSED (new shard auto-started after prior batch completion):
    - Classify as RED, not AMBER. Reason: auto-start proves control plane is still active and can launch arbitrary new runners.
    - Set stage7_human_quarantine_required=true.
    - Reset post_stage7_red_nonred_count to 0 (prior AMBER samples invalidated by new run-id).
    - Record the new run-id, shard-id, and start time in evidence.
    - The recovery gate must now wait for: (1) this new shard to complete/terminate, (2) no further auto-starts for ≥2 consecutive watchdog samples, (3) commander/control plane count=0 or human quarantine recorded.

  Watchdog prompt fragment to add:
    - After detecting MAINTENANCE_WINDOW_READY, the NEXT watchdog sample must check for new shard runners. If any new shard started (new run-id/shard-id not seen in prior samples), escalate to RED/MAINTENANCE_WINDOW_MISSED_RELAUNCH.
    - Track `shard_advancement_detected` field: true when current sample shows a shard-id > max shard-id from prior samples.
    - Track `consecutive_amber_without_shard_advancement`: count of AMBER samples where no new shard appeared. Recovery requires ≥2.

  ## Part B: Daily-executor — run-state freshness bridge

  The daily executor runs only once at 12:00, but state changes (batch completion, new shard starts) happen throughout the day. Add to daily-executor prompt:
    - When reading latest watchdog reports, if the most recent watchdog shows a materially different state than run-state.json (e.g., BATCH001 completed, shard005 started, commander dead), UPDATE run-state.json to reflect the latest verified state even if no story is executed.
    - Specifically update: current_phase, prompt_patches_applied (new patch integrated), last_watchdog, latest_daily_executor metrics.
    - This is a P1 operation (per session budget table): ~30s to refresh run-state from watchdog evidence.

  ## Part C: Recovery gate refinement (amends #36)

  The #36 recovery gate requires:
    writer_count=0, commander/control=0, no new batch/shard/run-id, ≥2 consecutive non-RED samples.

  Amendment: Add explicit MAINTENANCE_WINDOW_MISSED handling:
    - If any watchdog in the post-RED window reports MAINTENANCE_WINDOW_MISSED (new shard auto-start), the recovery counter resets to 0.
    - The "no new batch/shard/run-id" criterion must be checked against ALL watchdog samples since the last RED, not just the current sample.
    - Add field `max_shard_id_seen_since_last_red` to track shard advancement across the recovery window.

  ## Part D: Watchdog — liveness fields still missing

  Per prior prompt-review #36 suggestion, watchdog should consistently emit:
    daily_executor_age_h, latest_daily_session_file, current_day_daily_artifact_present, executor_artifact_gap_risk.
  Current watchdogs (1612, 1645) still omit these. Add to watchdog prompt as mandatory fields.

next_cycle_actions: |
  1. Next watchdog should check shard005 progress: if still running, classify RED/ACTIVE_WRITER; if completed with no new shard, classify AMBER with consecutive_amber_without_shard_advancement=1.
  2. Next daily-executor (tomorrow 12:00) must: integrate patch #37 into ledger, refresh run-state from latest watchdog evidence, evaluate shard005 outcome, and re-assess recovery gate with amended #36+#37 criteria.
  3. No story execution possible while shard005 or any Stage7/93K writer is active, or while WeChat process_count=0.
  4. If shard005 completes and no new shard auto-starts for 2+ watchdog cycles, the recovery gate may open for US-003 no-capture status artifact (not full capture).
```

## Evidence collected
- run-state: status=`running`; phase=`P1 Day 6 ACTIVE_STAGE7_93K_COMMANDER_RELAUNCH_LOCKDOWN`; last_updated=`2026-05-04T12:02:41+08:00` (stale 4.8h).
- Latest daily executor: 2026-05-04 12:02, decision=RED, classification=STAGE7_93K_COMMANDER_RELAUNCH_LOCKDOWN_RED, patch #36 integrated.
- Watchdog trend (last 6h, 10:48→16:45):
  - 10:48 RED: writer=1, commander=1, 93k_runner=1
  - 11:22 RED: active writers
  - 11:55 RED: active writers
  - 12:29 RED: active writers
  - 13:02 RED: active writers
  - 13:35 RED: active writers
  - 15:40 RED: commander PID 117128 + shard004 runner PID 142156 alive
  - 16:12 AMBER: MAINTENANCE_WINDOW_READY — all runners dead, BATCH001 complete (358/400, 41 valid_skip, 1 failed_final, 1 parse_fail)
  - 16:45 AMBER: MAINTENANCE_WINDOW_MISSED — shard005 auto-started at 16:43, single runner, no duplicate
- Latest prompt-review: prompt-review-2026-05-04_1051.md (patch #36).
- Cron sessions matching hermes-7day: 1110.
- Prior prompt-reviews: 25 total since 2026-04-28.

## Self-critique
Patch #36 correctly generalized the Stage7/93K commander relaunch gate beyond NEXT1000 naming. However, it did not anticipate the MAINTENANCE_WINDOW_MISSED pattern: the control plane waits for all current shards to complete, then launches the next shard. This means a zero-writer snapshot is NOT a recovery signal — it's just the gap between shard completions and the next auto-start. The recovery gate must track shard advancement across the entire recovery window, not just the instantaneous writer count. The 16:12→16:45 transition (AMBER→new shard) is the exact failure mode #36's recovery criteria didn't cover.

Additionally, the daily-executor's once-daily schedule (12:00) creates a structural blind spot: state changes between 12:00 and the next day's run are only captured by watchdogs (read-only), leaving run-state.json stale for up to 24h. The daily-executor should treat run-state refresh from watchdog evidence as a P1 mandatory step, even when no story is executed.
