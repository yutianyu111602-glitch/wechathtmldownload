# Hermes 7-Day Watchdog RED — 2026-05-01T02:04:51+08:00

## Summary
- watchdog: RED
- reason: active live-root recursive scan is still running under OpenClaw `ocr-report.sh`.
- need_human: true
- action_taken: read-only verification only; no kill/restart/delete/cleanup performed.

## SOLVE LOOP snapshot
- run_state: `running`
  - current_phase: `P1 Day 1 baseline/context gate recovery`
  - current_story: `US-000 (completed), US-001 (completed), D0-MAINT (completed), US-002 Baseline Freeze (completed)`
  - next_story: `US-003 Context Gate / capture queue (WeChat-gated; skip capture while WeChat RED)`
  - state_mtime: `2026-04-30T12:08:37.400792+08:00`
- disk: `D:\              15T  6.3T  8.4T  43% /mnt/d` — GREEN
- llama-swap: UP by Windows PowerShell `Invoke-WebRequest` HTTP 200; 14 models returned including `Qwen3.6-27B`.
  - WSL `curl http://127.0.0.1:11434/v1/models` returned rc=7, consistent with known WSL2 localhost isolation; not used as DOWN classification.
- checkpoint/report freshness: latest report `WATCHDOG_RED_2026-05-01_0132.md`, age `32.3` minutes — fresh for 30m-ish watchdog cadence.
- output dir: `0	/mnt/d/HTML/hermes-longrun-2026-04-28/` — unchanged/idle.
- Windows-native D-touch scan: no matching `D:\DDownload` / `D:\HTML` large scan process found by CIM.

## RED evidence — active live-root recursive scan

Initial WSL process sample found one live-root recursive file operation:

```text
pc         13747  1.8  0.0   5392  3048 ?        R    02:00   0:05 find /mnt/d/DDownload/_llm_artifacts -name poster_ocr.json -mmin -10 -exec grep -l "backend": "easyocr" {} ;
```

Immediate PID re-check confirmed the same process was still alive and in uninterruptible sleep (`D`) under the OpenClaw helper chain:

```text
13737   13736 Ss   sh              /bin/sh -c /home/pc/.openclaw/workspace/ocr-report.sh >> /tmp/ocr-report.log 2>&1
13738   13737 S    ocr-report.sh   /bin/bash /home/pc/.openclaw/workspace/ocr-report.sh
13747   13738 D    find            find /mnt/d/DDownload/_llm_artifacts -name poster_ocr.json -mmin -10 -exec grep -l "backend": "easyocr" {} ;
```

Fresh targeted sample still showed one active live-root recursive scan:

```text
FRESH_LIVE_ROOT_RECURSIVE_COUNT=1
pc         13747  1.8  0.0   4732  2412 ?        D    02:00   0:05 find /mnt/d/DDownload/_llm_artifacts -name poster_ocr.json -mmin -10 -exec grep -l "backend": "easyocr" {} ;
```

Persistent OpenClaw processes observed; token values redacted:

```text
pc           397  0.0  0.2 1292840 48660 ?       Ssl  Apr30   0:01 infisical run --token=REDACTED --env=dev -- /usr/local/bin/node /home/pc/.npm-global/lib/node_modules/openclaw/dist/index.js gateway --port 18789
pc           400  0.0  1.6 1629112 268556 ?      Ssl  Apr30   0:06 openclaw-node
pc           805  3.0  5.8 19357872 951164 ?     Sl   Apr30  11:14 openclaw-gateway
pc         13737  0.0  0.0   2800  1536 ?        Ss   02:00   0:00 /bin/sh -c /home/pc/.openclaw/workspace/ocr-report.sh >> /tmp/ocr-report.log 2>&1
pc         13738  0.0  0.0   4752  3072 ?        S    02:00   0:00 /bin/bash /home/pc/.openclaw/workspace/ocr-report.sh
```

## REVIEW LOOP mini review

The latest daily executor report (`daily-executor-2026-04-30_120715.md`) marked US-002 GREEN and completed the evidence/checkpoint artifact contract, but the current environment remains RED because the OpenClaw `ocr-report.sh` helper is actively running a recursive `find ... -exec grep` against `D:\DDownload\_llm_artifacts`; next prompt/ops adjustment should quarantine/disable that helper before US-003 proceeds.

## Decision

RED is valid by current live process evidence. Stop for human handling; do not advance stages, start capture/downstream jobs, or modify live-root artifacts until the active recursive scan is handled.
