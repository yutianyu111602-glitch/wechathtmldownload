# 🔍 DeepTutor Prompt Review — 2026-05-01 16:13 +08

```text
prompt-review: 2026-05-01T16:13:20+08:00
cycles_this_period: 11 watchdog samples in last 6h; cron_history_matches=573; hermes_7day_sessions_last_6h=46
watchdog_status: RED (6次) + AMBER (5次); current=RED; latest3=RED/RED/RED
stories_completed: 1 partial/safe fallback artifact set (US-003 status + context-gate/queue-plan at 12:03), but latest executor cursor still deferred/gated and no capture started
blocking_issues:
  - Repeated RED: latest watchdogs 14:41, 15:14, 15:47 all confirm active OpenClaw-derived Stage7 process chain writing under /mnt/d/downstream_results with wrapper command including `rm -rf "$OUT_DIR"`.
  - Regression after 12:05 executor: executor integrated Patch #23 and saw current_live_root_danger=0, but subsequent watchdogs detected an active Stage7/downstream_results job. US-003 must remain gated until this clears and hysteresis passes.
  - Persistent OpenClaw gateway/node processes remain present; watchdog evidence reports openclaw-node/infisical/node plus Stage7 child bash/python/tee.
  - WeChat remains effectively gated for capture; run-state says process not found / capture skipped, while one executor sample saw process_count=2 but still did not authorize capture.
  - mem0 remains AMBER: Provider `(none — built-in only)` in run-state; do not claim Mem0 Cloud writes.
prompt_changes_needed: true
suggested_patches:
  - Patch #24 [CRITICAL / daily-executor hard blocker for OpenClaw Stage7 broad-output writers; builds on already-integrated #23]:
      Add before any story selection or US-003 acceptance:
        1. Scan WSL process table for OpenClaw-derived Stage7/canary wrappers and child `run_er_sample_full.py`/`tee`/bash commands, not only live-root recursive scans.
        2. Treat any active OpenClaw/Stage7 process writing to `/mnt/d/downstream_results`, `/mnt/d/agent-memory/reports/openclaw_*`, or using a wrapper containing `rm -rf "$OUT_DIR"` as RED blocker even when `/mnt/d/DDownload`, `/mnt/d/HTML`, and `/mnt/d/aidata` recursive live-root scans are clear.
        3. If blocker present: write RED evidence + checkpoint, keep US-003/capture/downstream gated, do not kill/restart/delete/repair, require human quarantine/stop decision.
        4. If blocker absent but RED occurred in last 6h: require >=2 consecutive non-RED watchdog samples after the last RED before executing even safe US-003 fallback artifact work.
        5. Acceptance for WeChat-RED US-003 remains status/context-gate/queue artifacts only; no capture launch and no output-dir growth requirement.
      Rationale: Patch #23 caught live-root scans and was integrated at 12:05, but a new recurring RED class appeared immediately afterward: authorized-looking OpenClaw Stage7 child jobs touching broad downstream output roots. The prompt must name this class explicitly so daily-executor cannot misclassify `current_live_root_danger=0` as sufficient safety.
  - Watchdog prompt patch [HIGH / explicit recurring Stage7 blocker fields]:
      Add fields to every watchdog report while this condition persists:
        `openclaw_stage7_active_count`, `openclaw_stage7_downstream_writer_count`, `last_stage7_red_watchdog`, `post_stage7_red_nonred_count`, and `stage7_human_quarantine_required`.
      Keep existing no-auto-kill/no-auto-fix behavior.
next_cycle_actions: Daily-executor should integrate Patch #24 before any story work. If any OpenClaw Stage7/downstream_results writer is still alive, write RED checkpoint/evidence and stop. If clear, wait for/verify >=2 consecutive non-RED watchdog samples after the last RED, then only resume US-003 no-capture fallback artifacts. Next prompt-review should verify whether Patch #24 entered the ledger and whether RED frequency dropped.
```

## Evidence collected
- run-state: `/mnt/c/code/githubstar/wechathtmldownload/docs/longrun/hermes-7day/state/run-state.json` status=`running`, last_updated=`2026-05-01T12:05:00+08:00`, latest_daily_executor decision=`AMBER`, story=`US-003 deferred by live-root RED hysteresis gate`, metrics include watchdog_red_6h=2, post_red_nonred=1, current_live_root_danger=0, llama_swap_model_count=14, mem0_provider=`(none — built-in only)`.
- requested recent reports equivalent to `ls -lt ... | head -5`:
  - `WATCHDOG_RED_2026-05-01_1547.md`
  - `WATCHDOG_RED_2026-05-01_1514.md`
  - `WATCHDOG_RED_2026-05-01_1441.md`
  - `WATCHDOG_RED_2026-05-01_1408.md`
  - `WATCHDOG_AMBER_2026-05-01_1334.md`
- latest 3 watchdog outputs: all RED; all cite active OpenClaw-derived Stage7/downstream_results writer and require human handling.
- last-6h watchdog counts: 11 total = 6 RED + 5 AMBER.
- cron history equivalent to requested grep count: 573 `session_cron_*.json` files contain `hermes-7day`; live last-6h matching sessions observed: 46.
- story progress: US-003 safe fallback artifacts exist (`wechat-status-2026-05-01_120313.md`, `capture-queue-plan-2026-05-01_120313.md`, evidence/checkpoints), but latest run-state still marks US-003 deferred due to gate; count as partial/safe fallback, not capture progress.

## Self-critique
Patch #23 worked for live-root scan hysteresis and ledger integration, but it was too narrow: it did not clearly identify OpenClaw Stage7 broad-output writers under `/mnt/d/downstream_results` as a RED class. The next executor prompt needs this concrete blocker and an explicit post-RED hysteresis rule keyed to Stage7/downstream writers, otherwise it may repeatedly see `current_live_root_danger=0` and attempt story work while another unsafe OpenClaw child job is alive.
