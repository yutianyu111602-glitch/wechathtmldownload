# 🔍 DeepTutor Prompt Review — 2026-05-01 22:16 +08

```text
prompt-review: 2026-05-01T22:16:06+08:00
cycles_this_period: 11 watchdog samples in last 6h; cron_history_matches=617
watchdog_status: AMBER (9次) + RED (2次); latest3=AMBER/AMBER/AMBER; current=AMBER
stories_completed: 0 completed in this 6h period; US-003 remains gated/deferred (safe fallback artifacts existed from 12:05, no capture/output growth)
blocking_issues:
  - Repeated AMBER: persistent OpenClaw gateway/node processes remain present in all latest watchdogs (openclaw_persistent_process_count=3).
  - Recent RED history: two RED watchdogs inside this 6h window (16:21, 16:54) documented OpenClaw-derived Stage7/downstream_results writers, including broad-output paths and parent wrapper with `rm -rf "$OUT_DIR"`.
  - Hysteresis status is improving but still gated by policy: latest samples show current live-root danger count 0, llama-swap GREEN, disk GREEN, checkpoint fresh; however watchdog counters still keep recurring_red_risk=true.
  - WeChat/capture remains gated; output dir remains 0 bytes and no capture should be launched while WeChat is RED/unstable and OpenClaw hygiene is unresolved.
  - mem0 remains AMBER from run-state: Provider `(none — built-in only)`; do not claim Mem0 Cloud writes.
prompt_changes_needed: true
suggested_patches:
  - Consolidated carry-forward Patch #24 [CRITICAL / daily-executor]: integrate before any story selection. Scan WSL process table for OpenClaw-derived Stage7/canary wrappers and children (`run_er_sample_full.py`, `tee`, wrapper `bash`, `infisical`, `openclaw`). Treat writers to `/mnt/d/downstream_results`, `/mnt/d/agent-memory/reports/openclaw_*`, or wrappers containing `rm -rf "$OUT_DIR"` as RED blockers even if live-root recursive scan count is 0. If present: write RED evidence/checkpoint, keep US-003/capture/downstream gated, do not kill/restart/delete/repair, require human quarantine/stop decision.
  - Watchdog prompt refinement [HIGH / no net-new risk class]: latest watchdogs still expose only `openclaw_live_root_scan_*` counters. Add explicit machine fields every cycle: `openclaw_stage7_active_count`, `openclaw_stage7_downstream_writer_count`, `last_stage7_red_watchdog`, `post_stage7_red_nonred_count`, `stage7_human_quarantine_required`. This avoids conflating "no active live-root scan" with "no Stage7 broad-output writer".
  - Daily-executor ledger requirement [HIGH]: at next daily run, update run-state/checkpoint ledger for Patch #24 as `integrated` or `deferred`; if deferred, explain why. Do not create more patch IDs for this same condition unless a genuinely new blocker class appears.
next_cycle_actions: Daily-executor should first integrate/ledger Patch #24, run Stage7 broad-output writer scan, then apply hysteresis. If active Stage7/downstream writer exists, stop with RED evidence. If absent and at least 2 consecutive non-RED watchdog samples after the last Stage7 RED are verified, it may perform only a minimal US-003 no-capture fallback/status artifact; do not start capture, Stage7, downstream, OCR, or graph expansion.
```

## Evidence collected
- run-state: `status=running`, `last_updated=2026-05-01T12:05:00+08:00`, latest daily executor decision `AMBER`, story `US-003 deferred by live-root RED hysteresis gate`, llama-swap model count 14, WeChat process count 0, mem0 provider `(none — built-in only)`.
- requested `ls -lt ... | head -5` equivalent: latest reports are `WATCHDOG_AMBER_2026-05-01_2150.md`, `WATCHDOG_AMBER_2026-05-01_2116.md`, `WATCHDOG_AMBER_2026-05-01_2043.md`, `WATCHDOG_AMBER_2026-05-01_2010.md`.
- latest 3 watchdogs (20:43, 21:16, 21:50): all AMBER; all report disk 8.4T free, llama-swap UP via Windows PowerShell, current danger count 0, persistent OpenClaw process count 3, and recurring red risk due recent RED history.
- last-6h watchdog count from filenames 16:16–22:16: 11 total = 2 RED + 9 AMBER.
- cron execution history requested grep count: 617 `session_cron_*.json` files contain `hermes-7day`.

## Self-critique
The system has improved from RED to AMBER after the 16:54 Stage7 writer disappeared, but the prompts still risk ambiguity: current watchdog text says "no active live-root danger" while only implicitly covering Stage7 broad-output writers. The next executor must not interpret `current_live_root_danger=0` as sufficient; it needs a named Stage7/downstream writer gate and a ledger entry for Patch #24.
